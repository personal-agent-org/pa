{"default":{"identifier":"default","description":"Core permissions for the main window plus the narrow `native_bridge` IPC command (fixed vocabulary: config / notifications / open-external). remote.urls is a wildcard because the SPA origin is set at runtime; the real guard is the `on_navigation` allowlist in lib.rs, which only loads the chosen instance and the exact external OIDC origin advertised by its discovery endpoint.","remote":{"urls":["http://*/*","https://*/*","http://*:*/*","https://*:*/*"]},"local":true,"windows":["main"],"permissions":["core:default"]}}
