{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window plus the narrow `native_bridge` IPC command (fixed vocabulary: config / notifications / open-external). remote.urls is a wildcard because the SPA origin is set at runtime; the real guard is the `on_navigation` allowlist in lib.rs, which only ever loads the chosen instance + its same-domain Keycloak in the webview, so only those origins can reach the bridge.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://*/*", "https://*/*", "http://*:*/*", "https://*:*/*"]
  },
  "permissions": ["core:default"]
}
